/**
 * A sum function to test JSDocs
 * @param {Number} a - the first number to add
 * @param {Number} b - the second number to add
 * @returns {Number} The sum of the two parameters
 */
function sum(a, b) {
  return a + b;
}
